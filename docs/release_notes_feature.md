Create a feature that opens a release note the first 5 times the user opens the app after an update. This will be a popup window that will be styled to match the app and the user can just click out of.

This release note will be made by me and will be in a markdown format. I am not sure how to package this with a new release of the application so give me some ideas on how best to write a release note and package it with the application.

There should also be a backlog of all the old release notes so that I can write a gemini command that can start the new release note for me.
