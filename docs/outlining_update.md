create a branch called outlining_update and add the following updates to the outlining feature.

1. Change division level
- currently in the reading view, you can change the level of a division by holding 'd' and scrolling on a division. This is good and correct. The rules for what levels a division can take a properly handled by this feature.
- create another method of division change that uses the up and down arrow keys. This should work exactly like the holding 'd' and scrolling up or down. But this additional method lets you simply hover over the division and hit the up or down arrow keys to change the division level using the same rules as the 'd' and scroll.

2. Move to note
- In the outline editor, the little header right now looks like:
[currently editing in reader]    [ref style]

- It should be updated to the following
Top of the page: 
[currently editing in reader]    [send to note]

Bottom of the page:
[ref style]

- The send to note button should create a note titled the same thing as the current outline that is populated with all of the information and styling from the outline, but using the Notes list system.
-Just as you can export outlines to turn them into a text file, exporting to notes should do the same thing.